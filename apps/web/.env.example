# Angular reads these at build time via src/environments.
# For local dev, copy the values into src/environments/environment.development.ts
# (do NOT put the service role key here — the web app only uses the anon key).

SUPABASE_URL=
SUPABASE_ANON_KEY=
API_BASE_URL=http://localhost:3000
